<UserControl x:Class="TuPos.App.Views.POSView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TuPos.App.Views"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Top Row: Barcode scanning & Search input -->
        <Grid Grid.Row="0" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <TextBox x:Name="TxtSearch" Grid.Column="0" 
                     Text="{Binding SearchQuery, UpdateSourceTrigger=PropertyChanged}" 
                     PlaceholderText="Escanea código de barras o escribe para buscar..."
                     KeyDown="TxtSearch_KeyDown"/>
            
            <Button Grid.Column="1" Content="Agregar Producto" Style="{StaticResource AccentButton}" 
                    Margin="15,0,0,0" Height="40" Padding="20,0"
                    Command="{Binding AddToCartCommand}" CommandParameter="{Binding SearchQuery}"/>
        </Grid>

        <!-- Middle Row: Shopping Cart Items Grid -->
        <Border Grid.Row="1" Background="{StaticResource BrushBgCard}" CornerRadius="12" BorderBrush="{StaticResource BrushBorder}" BorderThickness="1" Padding="15">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="Carrito de Ventas" FontSize="18" FontWeight="Bold" Foreground="White" Margin="0,0,0,15"/>

                <DataGrid Grid.Row="1" ItemsSource="{Binding Cart}" AutoGenerateColumns="False" 
                          CanUserAddRows="False" CanUserDeleteRows="False" SelectionMode="Single"
                          Background="Transparent" BorderThickness="0" RowBackground="Transparent"
                          Foreground="White" GridLinesVisibility="None" RowHeaderWidth="0" FontSize="14">
                    
                    <DataGrid.Resources>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="Foreground" Value="{StaticResource BrushTextSec}"/>
                            <Setter Property="FontWeight" Value="SemiBold"/>
                            <Setter Property="Padding" Value="5,10"/>
                            <Setter Property="BorderThickness" Value="0,0,0,1"/>
                            <Setter Property="BorderBrush" Value="{StaticResource BrushBorder}"/>
                        </Style>
                        <Style TargetType="DataGridRow">
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="BorderThickness" Value="0,0,0,1"/>
                            <Setter Property="BorderBrush" Value="#2d3748"/>
                            <Setter Property="Padding" Value="0,5"/>
                        </Style>
                        <Style TargetType="DataGridCell">
                            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="DataGridCell">
                                        <Border Padding="5,12" Background="{TemplateBinding Background}">
                                            <ContentPresenter VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </DataGrid.Resources>

                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Código" Binding="{Binding Code}" Width="150" IsReadOnly="True"/>
                        <DataGridTextColumn Header="Producto" Binding="{Binding Name}" Width="*" IsReadOnly="True"/>
                        
                        <!-- Editable Quantity Column -->
                        <DataGridTemplateColumn Header="Cant" Width="100">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBox Text="{Binding Quantity, UpdateSourceTrigger=PropertyChanged}" 
                                             Width="70" Height="30" HorizontalContentAlignment="Center" 
                                             VerticalContentAlignment="Center" FontSize="13" Padding="0"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <DataGridTextColumn Header="Precio" Binding="{Binding UnitPrice, StringFormat=C2}" Width="100" IsReadOnly="True"/>
                        <DataGridTextColumn Header="Subtotal" Binding="{Binding Subtotal, StringFormat=C2}" Width="120" IsReadOnly="True"/>
                        
                        <!-- Remove button column -->
                        <DataGridTemplateColumn Header="Acciones" Width="90">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Content="✕" Style="{StaticResource DangerButton}" Width="32" Height="32" Padding="0"
                                            Command="{Binding DataContext.RemoveFromCartCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                            CommandParameter="{Binding}"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>

        <!-- Bottom Row: Checkout panel -->
        <Grid Grid.Row="2" Margin="0,20,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="320"/>
            </Grid.ColumnDefinitions>

            <!-- Notification Messages -->
            <TextBlock Grid.Column="0" Text="{Binding CheckoutMessage}" Foreground="{StaticResource BrushWarning}" 
                       FontSize="15" FontWeight="SemiBold" VerticalAlignment="Center" Margin="10,0"/>

            <!-- Total and Checkout Controls -->
            <StackPanel Grid.Column="1" HorizontalAlignment="Right" Width="320">
                <Grid Margin="0,0,0,15">
                    <TextBlock Text="TOTAL VENTA" FontSize="14" Foreground="{StaticResource BrushTextSec}" VerticalAlignment="Bottom"/>
                    <TextBlock Text="{Binding Total, StringFormat=C2}" FontSize="36" FontWeight="Bold" Foreground="#4ade80" HorizontalAlignment="Right"/>
                </Grid>
                
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="100"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    
                    <Button Grid.Column="0" Content="Limpiar" Style="{StaticResource DangerButton}" Height="45" Margin="0,0,10,0"
                            Command="{Binding ClearCartCommand}"/>
                    <Button Grid.Column="1" Content="Cobrar (F12)" Style="{StaticResource AccentButton}" Height="45"
                            Command="{Binding CheckoutCommand}"/>
                </Grid>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
