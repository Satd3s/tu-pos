<UserControl x:Class="TuPos.App.Views.ReportsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Reportes y Estadísticas" FontSize="22" FontWeight="Bold" Foreground="White" Margin="0,0,0,30"/>

        <!-- Stat cards container -->
        <UniformGrid Grid.Row="1" Columns="2" Rows="2" Margin="-10">
            
            <!-- Card 1: Total Products -->
            <Border Background="{StaticResource BrushBgCard}" CornerRadius="12" BorderBrush="{StaticResource BrushBorder}" BorderThickness="1" Margin="10" Padding="25">
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="📦" FontSize="42" Margin="0,0,0,15"/>
                    <TextBlock Text="Total de Productos" FontSize="14" Foreground="{StaticResource BrushTextSec}"/>
                    <TextBlock Text="{Binding TotalProducts}" FontSize="36" FontWeight="Bold" Foreground="White" Margin="0,5,0,0"/>
                </StackPanel>
            </Border>

            <!-- Card 2: Inventory Value -->
            <Border Background="{StaticResource BrushBgCard}" CornerRadius="12" BorderBrush="{StaticResource BrushBorder}" BorderThickness="1" Margin="10" Padding="25">
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="💰" FontSize="42" Margin="0,0,0,15"/>
                    <TextBlock Text="Valor Total del Inventario" FontSize="14" Foreground="{StaticResource BrushTextSec}"/>
                    <TextBlock Text="{Binding InventoryValue, StringFormat=C2}" FontSize="36" FontWeight="Bold" Foreground="#4ade80" Margin="0,5,0,0"/>
                </StackPanel>
            </Border>

            <!-- Card 3: Low Stock -->
            <Border Background="{StaticResource BrushBgCard}" CornerRadius="12" BorderBrush="{StaticResource BrushBorder}" BorderThickness="1" Margin="10" Padding="25">
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="⚠️" FontSize="42" Margin="0,0,0,15"/>
                    <TextBlock Text="Productos con Bajo Stock" FontSize="14" Foreground="{StaticResource BrushTextSec}"/>
                    <TextBlock Text="{Binding LowStockProducts}" FontSize="36" FontWeight="Bold" Foreground="{StaticResource BrushDanger}" Margin="0,5,0,0"/>
                </StackPanel>
            </Border>

            <!-- Card 4: Daily Sales -->
            <Border Background="{StaticResource BrushBgCard}" CornerRadius="12" BorderBrush="{StaticResource BrushBorder}" BorderThickness="1" Margin="10" Padding="25">
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="📈" FontSize="42" Margin="0,0,0,15"/>
                    <TextBlock Text="Ventas Totales del Día" FontSize="14" Foreground="{StaticResource BrushTextSec}"/>
                    <TextBlock Text="{Binding DailySalesTotal, StringFormat=C2}" FontSize="36" FontWeight="Bold" Foreground="#3b82f6" Margin="0,5,0,0"/>
                </StackPanel>
            </Border>

        </UniformGrid>
    </Grid>
</UserControl>
